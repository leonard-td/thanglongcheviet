/dist
.env
.DS_Store
/uploads
# file zip backup do admin Settings > Backup tạo ra
/.backups
# Medusa order exports and other private file-local uploads (PII — admin auth only)
/static/private-*
/.private-exports
# thư mục runtime của quá trình restore media (bản cũ giữ lại để quay lui)
/static/.old-*
/static/.restore-tmp
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa